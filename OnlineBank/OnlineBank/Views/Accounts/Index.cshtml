@model IEnumerable<OnlineBank.Models.Account>

@{
    ViewBag.Title = "User Accounts";
}

<h2>User Accounts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Balance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InterestRate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOpened)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateClosed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Debit", "Debit", new { id = item.AccountID }) |
            @Html.ActionLink("Credit", "Credit", new { id = item.AccountID }) 
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Balance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InterestRate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOpened)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateClosed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.AccountID }) |
            @Html.ActionLink("Details", "Details", new { id = item.AccountID }) |
            @Html.ActionLink("Close", "Delete", new { id = item.AccountID })
        </td>
    </tr>
}

</table>
